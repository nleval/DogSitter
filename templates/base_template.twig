<!DOCTYPE html>
<html lang="fr">
<head>
    {% block head %}
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="icon" type="image/png" href="images/logo_dogsitter.png">

    <!-- Google Fonts -->
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600&family=Montserrat:wght@500;700&display=swap" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600;700&display=swap" rel="stylesheet">

    <!-- Bootstrap CSS & JS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>

    <!-- Bootstrap Icons -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css" rel="stylesheet">

    <!-- Styles hover inline -->
    <style>
        * { box-sizing: border-box; }

        html, body {
            height: 100%;
            overflow-x: hidden; 
            width: 100%;
            margin: 0;
            padding: 0;
        }

        body {
            display: flex;
            flex-direction: column;
        }

        .container-fluid {
            flex: 1 0 auto; 
            overflow-x: hidden;
        }

        footer {
            flex-shrink: 0; 
        }

        /* Survol des liens et icônes */
        .nav-link:hover,
        .nav-link:hover i {
            color: #DDA15E !important;
        }

        /* Survol du bouton de recherche */
        #headerSearchBtn:hover {
            background-color: #e6d9bf !important;
            color: #2b3b17 !important;
        }
    </style>

    <title>{% block title %}DogSynergie{% endblock %}</title>
    {% endblock %}
</head>
<body>

<div class="container-fluid px-0">

    <!-- HEADER -->
    <header class="row">
        <nav class="navbar navbar-expand-lg shadow-sm" style="background-color: #537031; position: relative;">
            <div class="container-fluid">
                <!-- Logo -->
                <a class="navbar-brand d-flex align-items-center" href="index.php" style="color: #FEFAE0; font-weight: 600;">
                    <img src="images/logo_dogsitter.png" alt="Logo DogSitter" height="45" class="me-2">
                    DogSynergie
                </a>

                <!-- Toggle button -->
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent"
                        aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Basculer la navigation">
                    <span class="navbar-toggler-icon"></span>
                </button>

                <!-- Navbar content -->
                <div class="collapse navbar-collapse" id="navbarSupportedContent">
                    <!-- Barre de recherche centrée -->
                    <div class="position-absolute top-50 start-50 translate-middle" style="left:50%; transform:translate(-50%,-50%); width:90%; max-width:520px; z-index:30;">
                        <form class="d-flex" onsubmit="return false;">
                            <div class="input-group rounded-pill overflow-hidden border" style="border-color: rgba(43,59,23,0.06);">
                                <input id="headerSearch" class="form-control" placeholder="Tapez votre recherche" aria-label="Recherche rapide" autocomplete="off"
                                       style="border: none; background-color: #FEFAE0; color: #2b3b17; padding: 0.5rem 1rem;">
                                <button id="headerSearchBtn" class="btn" type="button"
                                        style="border: none; background-color: #FEFAE0; color: #2b3b17; padding: 0.5rem 1rem;">Recherche</button>
                            </div>
                        </form>
                    </div>

                    <!-- Icônes à droite (minimal) -->
                    <ul class="navbar-nav ms-auto mb-2 mb-lg-0 d-flex align-items-center">
                        {% if session is defined and session.user is defined %}
                        <li class="nav-item me-2">
                            <a class="nav-link" href="index.php?controleur=conversation&methode=afficherAllConversation" aria-label="Messages"
                               style="color: #FEFAE0;">
                                <i class="bi bi-chat-dots fs-5" style="color: #FEFAE0;"></i>
                                <span class="visually-hidden">Messages</span>
                            </a>
                        </li>
                        <li class="nav-item me-2">
                            <a class="nav-link" href="index.php?controleur=utilisateur&methode=afficherUtilisateur&id_utilisateur={{ session.user.id_utilisateur }}" aria-label="Mon profil" style="color: #FEFAE0; font-weight:600;">
                                {{ session.user.pseudo }}
                            </a>
                        </li>
                        {% else %}
                        <li class="nav-item me-2">
                            <a class="nav-link" href="index.php?controleur=conversation&methode=afficherAllConversation" aria-label="Messages"
                               style="color: #FEFAE0;">
                                <i class="bi bi-chat-dots fs-5" style="color: #FEFAE0;"></i>
                                <span class="visually-hidden">Messages</span>
                            </a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link {% if menu=='auth' %}active{% endif %}" href="index.php?controleur=utilisateur&methode=authentification" aria-label="Se connecter / S'inscrire"
                               style="color: #FEFAE0; display: flex; align-items: center;">
                                <i class="bi bi-person-circle fs-5 me-2" style="color: #FEFAE0;"></i>
                                <span class="visually-hidden">Se connecter / S'inscrire</span>
                            </a>
                        </li>
                        {% endif %}
                    </ul>

                </div>
            </div>
        </nav>
    </header>

    <!-- CONTENT -->
    {% block content %}{% endblock %}

    <!-- FOOTER -->
    {% block footer %}
    <footer class="text-center py-3 shadow-sm" style="background-color: #537031; color: #FEFAE0;">
        <p class="mb-1">&copy; {{ "now"|date("Y") }} DogSitter. Tous droits réservés.</p>
        <p class="mb-0">
            <a href="confidentialité.php" style="color: #DDA15E; text-decoration: none;">Politique de confidentialité</a> |
            <a href="conditionUtilisation.php" style="color: #DDA15E; text-decoration: none;">Conditions d'utilisation</a>
        </p>
    </footer>
    {% endblock %}

    {% block script %}{% endblock %}
</div>

</body>
</html>

<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>Accueil</title>
</head>
<body>
    {% if avisListe is defined and avisListe|length > 0 %}
        <h2>Liste des avis :</h2>
        <ul>
            {% for avis in avisListe %}
                <li>{{ avis.note }} - {{ avis.textecommentaire }} - {{ avis.idutilisateur }} - {{ avis.idutilisateurnote }}</li>
            {% else %}
                <li>Aucun avis trouvé.</li>
            {% endfor %}
        </ul>

    {% elseif avis is defined %}
        <h2>Un avis :</h2>
        <ul>
            <li>{{ avis.note }} - {{ avis.textecommentaire }} - {{ avis.idutilisateur }} - {{ avis.idutilisateurnote }}</li>
        </ul>
    {% else %}
        <p>Aucun avis à afficher.</p>
    {% endif %}

    <a href="?controleur=Index&methode=render">Accueil</a>
</body>
</html>
